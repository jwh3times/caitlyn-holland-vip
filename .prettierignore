# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
.next/
out/
build/
dist/

# Misc
.DS_Store
*.pem

# Agent scratch (gitignored, never reaches CI)
.claude/settings.local.json
# Agent worktrees are full repo checkouts. Git skips them via .git/info/exclude,
# but Prettier does not read that file, so without this line `format:check` walks
# into every checkout and reports another branch's files as local style failures.
.claude/worktrees/

# Generated by scripts/sync-agents.mjs. `sync:agents:check` compares them byte-for-byte
# with the generator's output, so Prettier must never reformat them.
.claude/skills/
.codex/agents/
# Shared verbatim across repositories; never reformat locally.
scripts/sync-agents.mjs
scripts/sync-agents.test.mjs

# Logs
*.log

# Lock files
package-lock.json
yarn.lock
pnpm-lock.yaml
